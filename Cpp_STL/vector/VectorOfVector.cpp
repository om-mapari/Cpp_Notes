#include <iostream>
using namespace std;
#include <bits/stdc++.h>
int main()
{
    //IMAGINE V[i].ANYTHING
    //ANYTHING IS INSIDE VECTOR
    vector<vector<int>> v;
    vector<vector<int>> matrix(row, vector<int>(col)); //alociate zero matrix
    vector<vector<int>> mat(row, vector<int>(col, 0)); //alociate zero matrix

    return 0;
}